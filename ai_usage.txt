AI usage log — Practice 2
Date: 2026-09-14
Assistant: OpenAI Codex

The user asked for Task 1, asked to keep the lexer in compiler.py, requested a
requirements check, and then requested the repository requirements.

Codex read the assignment PDF and the existing local code. It completed the
hand-written byte state machine, added unclosed-brace checks, positioned number
errors at the first byte of the token, and added a --tokens command. It moved the
lexer into compiler.py, wrote lexer tests and run instructions, and checked the
worked example and lexical-error command output. All five unittest methods pass.

For repository setup, Codex recovered the retained earlier compiler as a Practice
1 baseline (the practice-1 files were empty), restored missing LLVM integer type
definitions, preserved the published README-only initial commit, created the
practice-2 branch, and moved compiler.py and tests/ to the repository root.

Task 2 token-based parsing and semantic checks have not been implemented.
This log summarizes the conversation; it is not a verbatim transcript.

Repository publication:
The recovered baseline and Task 1 commits were pushed to GitHub. Codex opened
https://github.com/nvytska/languages-and-compilers-design-2026/pull/1 into main.
GitHub rejected the review request because vovaskochko is not a collaborator.
Automatic approval review blocked a collaborator invitation pending explicit
user authorization. No invitation was sent. The public repository is readable
without an invitation. The pull request remains open and unmerged.

Task 2 — local implementation and verification:
The user requested Task 2 and required approval before adding GitHub comments
or other text. Codex replaced the old regex source parser with a token-only
line parser and llvmlite.ir code generation. It added mandatory initialisers,
const/mut enforcement, declaration-before-use checks, duplicate declarations,
exit constants and variables, extra-token checks, final-exit enforcement, and
positioned syntax errors. It added 7 valid and 18 invalid fixture programs with
expected output files, plus end-to-end compilation tests. It installed llvmlite
0.49.0 in the project .venv. All 7 unittest methods pass; valid LLVM modules
verify and compile/run using clang, and the assignment example prints 70.
Invalid programs were tested both with absent output files and with existing
output files to verify that failed compilation does not write output.
README run instructions were updated locally. Task 2 changes and proposed PR text were prepared locally for user approval.

Shared AI conversation (provided by the user):
https://chatgpt.com/share/6aa80ef9-da9c-83eb-bde3-5b882f92e2c0

The user supplied the shared conversation link, then authorized pushing the
Practice 2 changes and updating the existing pull request to cover both tasks.
